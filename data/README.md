# Data policy

Do not commit medical images, patient-level records, model weights, or downloaded datasets.

The default development flow uses generated aggregate site summaries. Synthetic NIfTI generation and
public dataset adapters will be added behind explicit commands. Public datasets must be downloaded on
the DGX Spark node and their license/source recorded in a manifest.

